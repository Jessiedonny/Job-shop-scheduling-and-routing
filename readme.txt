--This program is implemented in python 3--
--To run this program, naivagates to the folder where main.py sits, copy the below command line into the terminal to run it:

python3 main.py

--Plotted graph will show up one by one. Close the current one in order to see the next graph. 
--Brief information of the found solution will display in the terminal once the plotted graph are closed.

--NB: please make sure the data files are in the same folder as main.py, utility.py and loader.py